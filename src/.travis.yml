language: generic
sudo: required

services:
  - docker

before_install:
  - docker build -t stephengrider/react -f Dockerfile.dev .

script:
  - docker run -e CI=true stephengrider/react npm run test
